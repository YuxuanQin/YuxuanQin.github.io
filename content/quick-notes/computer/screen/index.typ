#import "/config.typ": template, tufted
#show: template.with(title: "GNU `screen` 怎么用")
= GNU `screen` 怎么用
<gnu-screen-怎么用>
根据某知乎老哥的建议，特来学习 `screen`，命令只有两条：

+ `screen -xRR`：进入已有的标签页，若没有则新建。
+ `ctrl-A, d`：退出标签页。

ok 就这么多！
